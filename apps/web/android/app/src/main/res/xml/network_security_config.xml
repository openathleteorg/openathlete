<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuration for api.openathlete.org -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.openathlete.org</domain>
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user added certificates (for debugging) -->
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
    
    <!-- Configuration for openathlete.org (main domain) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">openathlete.org</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
    
    <!-- Base configuration: only allow HTTPS by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
