enum user_role {
  ATHLETE
  COACH
}

enum event_type {
  TRAINING
  COMPETITION
  NOTE
  ACTIVITY
}

enum connector_provider {
  STRAVA
  GARMIN
  FITBIT
  APPLE_HEALTH
}

enum sport_type {
  RUNNING
  TRAIL_RUNNING
  CYCLING
  SWIMMING
  ROCK_CLIMBING
  HIKING
  STRENGTH
  CROSSFIT
  YOGA
  OTHER
}

enum training_zone_type {
  HEARTRATE
  POWER
  PACE
}

enum token_type {
  PASSWORD_RESET
}

enum record_type {
  SPEED
  HEARTRATE
  POWER
  ELEVATION_GAIN
  ELEVATION_LOSS
  CADENCE
}

enum agent_message_role {
  USER
  ASSISTANT
  SYSTEM
  TOOL
}

enum agent_message_status {
  pending
  processing
  completed
  error
}

enum agent_block_type {
  TEXT
  THINKING
  TOOL_CALL
  TOOL_RESULT
  CODE
  CHART_LINE
  CHART_BAR
  CHART_PIE
  CHART_SCATTER
  CHART_AREA
  TABLE
  MAP
  ACTIVITY_SUMMARY
  ACTIVITY_LIST
  ACTIVITY_CREATED
  TRAINING_PLAN
  ERROR
  IMAGE
  FILE
}

enum agent_block_status {
  pending
  processing
  completed
  error
  cancelled
}

enum equipment_type {
  SHOE
  BIKE
}

enum normalization_factor {
  SLOPE
  TEMPERATURE
  ALTITUDE
  WIND
  HUMIDITY
  RADIATION
}

enum metric_type {
  // Santé & composition corporelle
  WEIGHT
  HEIGHT
  BMI
  BODY_FAT
  MUSCLE_MASS
  DAILY_CALORIES
  DAILY_STEPS
  SLEEP_DURATION
  SLEEP_SCORE
  HOOPER_INDEX

  // Cardiaque
  HR_MAX
  HR_REST
  HR_RESERVE
  RMSSD

  // Performance & physiologie
  VMA
  CRITICAL_POWER_RUNNING
  CRITICAL_POWER_CYCLING
  VO2MAX
  FTP_RUNNING
  FTP_CYCLING
  VERTICAL_SPEED_AVG
  VERTICAL_SPEED_MAX
  FITNESS_INDEX
}

enum training_load_calculation_type {
  FOSTER_RPE // Foster method using RPE x duration
  TRIMP_EDWARDS // Edwards TRIMP using HR zones
  TRIMP_BANISTER // Banister TRIMP using exponential HR weighting
}

enum workout_step_type {
  WARMUP
  COOLDOWN
  INTERVAL_ACTIVE // work interval
  INTERVAL_REST // recovery interval
  STEADY // steady pace/effort
  REPEAT // container for repeated steps
  FREE // open/unstructured
}

enum workout_duration_type {
  TIME // duration in seconds
  DISTANCE // distance in meters
  CALORIES // calories to burn
  HR_BELOW // until HR drops below value
  HR_ABOVE // until HR rises above value
  LAP_BUTTON // manual lap press
  OPEN // no defined duration
  REPS // number of repetitions (for strength training)
}

enum workout_target_type {
  OPEN // no specific target
  PACE // pace/speed target
  SPEED // speed target (m/s)
  HEARTRATE // heart rate target
  POWER // power target
  CADENCE // cadence target
  RPE // rate of perceived exertion
  WEIGHT // weight/load target (for strength training)
  REPS_TARGET // target number of reps (alternative to duration)
}

enum workout_target_unit {
  MIN_PER_KM // pace in min/km
  M_PER_S // speed in m/s
  KM_PER_H // speed in km/h
  BPM // heart rate in beats per minute
  PERCENT_MAX_HR // percentage of max heart rate
  WATTS // power in watts
  PERCENT_FTP // percentage of functional threshold power
  RPM // cadence in revolutions per minute
  SPM // cadence in steps per minute
  RPE_SCALE // RPE on 1-10 scale
  KG // weight in kilograms
  LBS // weight in pounds
  REPS // number of repetitions
}

enum plan_status {
  DRAFT
  ACTIVE
  COMPLETED
  ARCHIVED
}

enum cycle_phase {
  BASE
  SPECIFIC
  TAPER
  RECOVERY
  COMPETITION
}

enum availability_priority {
  LOW
  MEDIUM
  HIGH
}
